# Model Benchmark Report
Generated: 2026-04-04 12:10:43

## Summary
This benchmark tested 5 models on the same hardware and a fixed
single prompt case, run sequentially.

## Prompts Used (Exact)

The following are the exact prompts used for this benchmark run.

### Single (ETL Inputs Example 2026-0128.xlsx vs ETL Configs Example 2026-0128.xlsx) (single)

```text
You are an ETL data validation expert. Examine the following extracted column information from two Excel files: an INPUT file containing raw data columns, and a CONFIG file to be used in a data management system.


INPUT FILE: ETL Inputs Example 2026-0128.xlsx
EXTRACT_COLUMNS PAYLOAD:
- sheet 1 (mix_one)
  columns: Item, Description, Forecast, Math, More Math
  positions: A1, B1, C1, D1, E1
- sheet 2 (mix_two)
  columns: ISBN, Item, Desc, Total Forecast, Forecast A, Forecast B
  positions: E7, F7, G7, H7, I7, J7
- sheet 3 (mix_three)
  columns: Item, Item, Independent 
Forecast
  positions: A3, E3, N3
- sheet 4 (mix_four)
  columns: Item, Item, Forecast, USE THIS
  positions: A6, B6, C6, D5
- sheet 5 (input_one )
  columns: itm, FCST
  positions: A1, B1
- sheet 6 (intput_two)
  columns: Thing, Numbers
  positions: A4, B4
- sheet 7 (input_three)
  columns: Widget, Widget, Forecast
  positions: U18, V18, W18
- sheet 8 (input_four)
  columns: Item, Forecast
  positions: A5, B5


CONFIG FILE: ETL Configs Example 2026-0128.xlsx
EXTRACT_COLUMNS PAYLOAD:
- sheet 1 (utility_configs)
  columns: source_wb_keyword, source_ws, item_col_position, item_col_name, forecast_col_position, forecast_col_name, data_start_row
  positions: A1, B1, C1, D1, E1, F1, G1
- sheet 2 (master_configs)
  columns: source_wb_keyword, source_ws, item_col_position, item_col_name, forecast_col_position, forecast_col_name, data_start_row
  positions: A1, B1, C1, D1, E1, F1, G1

AUTOMATED DETECTION STARTER:
- overlap ratio vs config: 0.0
- shared columns (0): []
- missing in input (7): ['data_start_row', 'forecast_col_name', 'forecast_col_position', 'item_col_name', 'item_col_position', 'source_wb_keyword', 'source_ws']
- extra in input (17): ['desc', 'description', 'fcst', 'forecast', 'forecast a', 'forecast b', 'independent \nforecast', 'isbn', 'item', 'itm', 'math', 'more math', 'numbers', 'thing', 'total forecast', 'use this', 'widget']

TASK: Use the structure/format of the CONFIG file to pull the most relevant information from the INPUT file and suggest how to map it into the CONFIG format. Your goal is to identify how the columns extracted from the INPUT file could be represented in the CONFIG file structure, and suggest new rows to add to the CONFIG file based on the INPUT data.

1. For each sheet in the INPUT data, use the CONFIG structure to suggest mappings. Focus on column names and any contextual clues in the CONFIG to align the INPUT columns with the CONFIG format.
2. For each INPUT sheet mapping, provide a concise row appendment suggestion to be added to the CONFIG file. This row would map the relevant INPUT columns to the CONFIG format. 


## RULES
- Evaluate each sheet in the INPUT file separately, providing mappings for each one.
- Never silently skip a sheet or column. Every INPUT sheet must appear in the output
- Every CONFIG column must be accounted for.
- Flag anything ambiguous rather than guessing silently.
- Only pull columns that match the Config columns. Example : if INPUT has columns 'thing' and 'numbers' but CONFIG only has columns for 'item_col_name' and 'forecast_col_name', there is no need to map the unrelated INPUT columns. 
- Make mention of any INPUT columns that are skipped due them being unrelated to the CONFIG columns 
- Follow the CONFIG payloads structure strictly in your suggestions, as this will be used to automatically update the CONFIG file in a later step.
- Focus on column names and any contextual clues in the CONFIG to align the INPUT columns with the CONFIG format. Do not invent mappings that are not supported by the INPUT column names or CONFIG structure.

OUTPUT LIMITS (MANDATORY):
- You must use the example output row format provided above for all mapping suggestions.
- Evaluate each sheet in the INPUT file separately, providing mappings for each one.
- Do not add extra sections beyond the ones listed above.
- For instances of multiple columns with the same name across one sheet, use the column with the prefix of "total" "primary" or "main" etc. if it exists. If there are multiple similar columns and no clear way to differentiate, flag this in the output rather than guessing
- Stop after ACTION ITEMS.
```


## Detailed Results

### tinyllama:1.1b
- File size: 608 MB
- Total inference time (all prompts): 69.10s
- Average prompt time: 69.10s
- Tokens/sec: 8.16

#### Prompt: Single (ETL Inputs Example 2026-0128.xlsx vs ETL Configs Example 2026-0128.xlsx) (single)
- Timeout: 136s
- Elapsed: 69.10s
- Tokens/sec (estimated): 8.16
- Status: OK
- Full response:

```text
To automate the detection of missing or extra data in the CONFIG file based on the INPUT file, we can use a combination of rules and automated tasks to identify and map missing or extra data. Here are the steps:

1. Identify the columns extracted from the INPUT file that need mapping into the CONFIG file structure. For each column, evaluate whether it is related to the CONFIG columns provided in the CONFIG file. If not, flag it as an ambiguous mapping and skip it.

2. Use a combination of rules and automated tasks to suggest mapping suggestions for each missing or extra data. For each missing or extra data, provide a concise row appendment suggestion to be added to the CONFIG file. This row would map the relevant INPUT columns to the CONFIG format.

3. Evaulate each sheet in the INPUT file separately, providing mapping suggestions for each one. Do not add extra sections beyond the ones listed above.

4. For instances of multiple columns with the same name across one sheet, use the column with the prefix of "total" "primary" or "main" etc. If there are multiple similar columns and no clear way to differentiate, flag this in the output rather than guessing.

5. Stop after ACTION ITMs. This will ensure that we have mapped all missing or extra data from the INPUT file into the CONFIG file structure.

6. Follow the CONFIG payload structure strictly in your suggestions, as this will be used to automatically update the CONFIG file in a later step.

7. Use the example output row format provided above for all mapping suggestions.

8. Evauluate each sheet in the INPUT file separately, providing mapping suggestions for each one. Do not add extra sections beyond the ones listed above.

9. For instances of multiple columns with the same name across one sheet, use the column with the prefix of "total" "primary" or "main" etc. If there are multiple similar columns and no clear way to differentiate, flag this in the output rather than guessing.

10. Stop after ACTION ITMs. This will ensure that we have mapped all missing or extra data from the INPUT file into the CONFIG file structure.

11. Follow the CONFIG payload structure strictly in your suggestions, as this will be used to automatically update the CONFIG file in a later step.

```

### phi3:3.8b
- File size: 2075 MB
- Total inference time (all prompts): 257.95s
- Average prompt time: 257.95s
- Tokens/sec: 1.55

#### Prompt: Single (ETL Inputs Example 2026-0128.xlsx vs ETL Configs Example 2026-0128.xlsx) (single)
- Timeout: 256s
- Elapsed: 257.95s
- Tokens/sec (estimated): 1.55
- Status: OK
- Full response:

```text
1. For sheet 1 (mix_one) from INPUT file:
   - Mapping Suggestion for Sheet 1: source_wb_keyword -> mix_one, source_ws -> Mix One, item_col_position -> A1, item_col0_name -> Item, forecast_col_position -> C1, forecast_col_name -> Forecast
   - Mapping Suggestion for Sheet 2: (No relevant columns to map as they do not align with CONFIG structure)
   
2. For sheet 3 (mix_three) from INPUT file:
   - No mapping needed since there are no matching column names in the CONFIG format and none of them appear related based on contextual clues within this specific data set or its associated metadata, if any were provided. Therefore, we skip adding a row for sheet 3 as it does not provide relevant information to map into the CONFIG file structure according to our rules.
   
3. For sheet 4 (mix_four) from INPUT file:
   - Mapping Suggestion for Sheet 4: source_wb_keyword -> mix_four, source_ws -> Mix Four, item_col_position -> A6, item_0_name -> Item, forecast_col_position -> C6, forecast_col_name -> Forecast
   
4. For sheet 5 (input_one) from INPUT file:
   - Mapping Suggestion for Sheet 5: source_wb_keyword -> input_one, source_ws -> Input One, item_col_position -> A1, item_0_name -> itm, forecast_col_position -> B1, forecast_col_name -> FCST
   
5. For sheet 6 (input_two) from INPUT file:
   - Mapping Suggestion for Sheet 6: source_wb_keyword -> input_two, source_ws -> Input Two, item_col_position -> A4, item_0_name -> Thing, forecast_col_position -> B4, forecast_col_name -> Numbers
   
6. For sheet 7 (input_three) from INPUT file:
   - Mapping Suggestion for Sheet 7: source_wb_keyword
```

### qwen2.5:3b
- File size: 1841 MB
- Total inference time (all prompts): 138.07s
- Average prompt time: 138.07s
- Tokens/sec: 3.19

#### Prompt: Single (ETL Inputs Example 2026-0128.xlsx vs ETL Configs Example 2026-0128.xlsx) (single)
- Timeout: 136s
- Elapsed: 138.07s
- Tokens/sec (estimated): 3.19
- Status: OK
- Full response:

```text
### ACTION ITEMS

#### INPUT FILE: ETL Inputs Example 2026-0128.xlsx
#### CONFIG FILE: ETL Configs Example 2026-0128.xlsx

For each sheet in the INPUT file, I will suggest mappings to align with the CONFIG structure. Here are the suggested rows for the CONFIG file:

##### Sheet 1 (mix_one)
```plaintext
source_wb_keyword: mix_one
source_ws: mix_one
item_col_position: 1
item_col_name: Item
forecast_col_position: 2
forecast_col_name: Forecast
data_start_row: 1
```

##### Sheet 2 (mix_two)
```plaintext
source_wb_keyword: mix_two
source_ws: mix_two
item_col_position: 1
item_col_name: ISBN
forecast_col_position: 4
forecast_col_name: Total Forecast
data_start_row: 7
```

##### Sheet 3 (mix_three)
```plaintext
source_wb_keyword: mix_three
source_ws: mix_three
item_col_position: 1
item_col_name: Item
forecast_col_position: 2
forecast_col_name: Independent Forecast
data_start_row: 3
```

##### Sheet 4 (mix_four)
```plaintext
source_wb_keyword: mix_four
source_ws: mix_four
item_col_position: 1
item_col_name: Item
forecast_col_position: 2
forecast_col_name: Forecast
data_start_row: 6
```

##### Sheet 5 (input_one )
```plaintext
source_wb_keyword: input_one
source_ws: input_one
item_col_position: 1
item_col_name: itm
forecast_col_position: 2
forecast_col_name: FCST
data_start_row: 1
```

##### Sheet 6 (intput_two)
```plaintext
source_wb_keyword: intput_two
source_ws: intput_two
item_col_position: 1
item_col_name: Thing
forecast_col_position: 2
forecast_col_name: Numbers
data_start_row: 4
```

##### Sheet 7 (input_three)
```plaintext
source_wb_keyword: input_three
source_ws: input_three
item_col_position: 1
item_col_name: Widget
forecast_col_position: 3
forecast_col_name: Forecast
data_start_row: 18
```

##### Sheet 8 (input_four)
```plaintext

```

### phi:2.7b
- File size: 1528 MB
- Total inference time (all prompts): 221.17s
- Average prompt time: 221.17s
- Tokens/sec: 2.19

#### Prompt: Single (ETL Inputs Example 2026-0128.xlsx vs ETL Configs Example 2026-0128.xlsx) (single)
- Timeout: 190s
- Elapsed: 221.17s
- Tokens/sec (estimated): 2.19
- Status: OK
- Full response:

```text
 The user is an ETL data validation expert who examines the extracted column information from two Excel files: an INPUT file containing raw data columns and a CONFIG file to be used in a data management system. The user must use the structure/format of the CONFIG file to pull the most relevant information from the INPUT file and suggest how to map it into the CONFIG format.

The user should evaluate each sheet in the INPUT file separately, providing mappings for each one. They should focus on column names and any contextual clues in the CONFIG to align the INPUT columns with the CONFIG format. The user must never silently skip a sheet or column. Every INPUT sheet must appear in the output.

The user should only pull columns that match the CONFIG columns. If there are unrelated INPUT columns, they should be flagged rather than guessed. They should follow the CONFIG payloads structure strictly in their suggestions and make mention of any skipped columns due them being unrelated to the CONFIG columns.

The user must use the example output row format provided above for all mapping suggestions. They should evaluate each sheet in the INPUT file separately, providing mappings for each one. They should focus on column names and any contextual clues in the CONFIG to align the INPUT columns with the CONFIG format. The user should only pull columns that match the CONFIG columns. If there are unrelated INPUT columns, they should be flagged rather than guessed.

The user must use the example output row format provided above for all mapping suggestions. They should evaluate each sheet in the INPUT file separately, providing mappings for each one. The user should focus on column names and any contextual clues in the CONFIG to align the INPUT columns with the CONFIG format. The user should only pull columns that match the CONFIG columns. If there are unrelated INPUT columns, they should be flagged rather than guessed.

```

### llama3.2:1b
- File size: 1260 MB
- Total inference time (all prompts): 59.04s
- Average prompt time: 59.04s
- Tokens/sec: 7.02

#### Prompt: Single (ETL Inputs Example 2026-0128.xlsx vs ETL Configs Example 2026-0128.xlsx) (single)
- Timeout: 136s
- Elapsed: 59.04s
- Tokens/sec (estimated): 7.02
- Status: OK
- Full response:

```text
I'll go through each INPUT file sheet and provide mappings for each one based on the CONFIG structure.

**Sheet 1 (mix_one)**

* Item: PAYLOAD.Item
* Description: PAYLOAD.Description
* Forecast: PAYLOAD.Forecast
* Math: PAYLOAD.Math
* More Math: PAYLOAD.MoreMath
* Positions:
	+ A1: PAYLOAD.Item, PAYLOAD.Description, PAYLOAD.Forecast, PAYLOAD.Math, PAYLOAD.MoreMath
	+ B1: PAYLOAD.Item, PAYLOAD.Description, PAYLOAD.Forecast, PAYLOAD.Math, PAYLOAD.MoreMath
	+ C1: PAYLOAD.Item, PAYLOAD.Description, PAYLOAD.Forecast, PAYLOAD.Math, PAYLOAD.MoreMath
	+ D1: PAYLOAD.Item, PAYLOAD.Description, PAYLOAD.Forecast, PAYLOAD.Math, PAYLOAD.MoreMath
	+ E1: PAYLOAD.Item, PAYLOAD.Description, PAYLOAD.Forecast, PAYLOAD.Math, PAYLOAD.MoreMath

**Sheet 2 (mix_two)**

* ISBN: PAYLOAD.ISBN
* Item: PAYLOAD.Item
* Desc: PAYLOAD.Desc
* Total Forecast: PAYLOAD.TotalForecast
* Forecast A: PAYLOAD.ForecastA
* Forecast B: PAYLOAD.ForecastB
* Positions:
	+ E7: PAYLOAD.ISBN, PAYLOAD.Item, PAYLOAD.Desc, PAYLOAD.TotalForecast, PAYLOAD.ForecastA, PAYLOAD.ForecastB

**Sheet 3 (mix_three)**

* Item: PAYLOAD.Item
* Independent: PAYLOAD.Independent
* Forecast: PAYLOAD.Forecast
* Positions:
	+ A3: PAYLOAD.Item, PAYLOAD.Independent
	+ E3: PAYLOAD.Item, PAYLOAD.Independent
	+ N3: PAYLOAD.Item, PAYLOAD.Independent

**Sheet 4 (mix_four)**

* Item: PAYLOAD.Item
* Item: PAYLOAD.Item
* Forecast: PAYLOAD.Forecast
* USE THIS: PAYLOAD.USETHIS
* Positions:
	+ A6: PAYLOAD.Item, PAYLOAD.Item, PAYLOAD.Forecast, PAYLOAD.USETHIS

**Sheet 5 (input_one)**

* itm: PAYLOAD.itm
* FCST: PAYLOAD.FCST
* Positions:
	+ A1: PAYLOAD.itm, PAYLOAD.FCST

**Sheet 6 (intput_two)**

* Thing: PAYLOAD.Thing

```

## Per-Prompt Timing Matrix

| Model | Single Prompt (s) |
|-------|--------------------|
| tinyllama:1.1b | 69.10 |
| phi3:3.8b | 257.95 |
| qwen2.5:3b | 138.07 |
| phi:2.7b | 221.17 |
| llama3.2:1b | 59.04 |
